import SwiftUI

extension Font {
    static let timer: Font = .system(
        size: 48,
        weight: .light,
        design: .rounded
    )

    static let actionButton: Font = .system(
        size: 20,
        weight: .bold,
        design: .rounded
    )
}

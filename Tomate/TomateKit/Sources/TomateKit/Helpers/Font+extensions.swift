import SwiftUI

extension Font {
    static let timer: Font = .system(
        size: 48,
        weight: .light,
        design: .rounded
    ).monospacedDigit()

    static let actionButton: Font = .system(
        size: 36,
        weight: .bold,
        design: .rounded
    )
}
